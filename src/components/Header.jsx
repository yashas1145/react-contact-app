import React from "react";
import style from "../style.module.css"

function Header () {
    return <header>
        <h1 className={style.headerTitle}>Contacts</h1>
    </header>
}

export default Header;